namespace RestCodeGenerator;

// Placeholder entry point. A later task replaces this with the real
// generator CLI (argument parsing, file emission).
internal static class Program
{
    private static int Main(string[] args)
    {
        _ = args;
        return 0;
    }
}