## What changed

## Why

## How was this tested?
- [ ] Automated tests added or updated (new behavior, plus a regression test that fails without the fix for a bug fix)
- [ ] `dotnet test` passes for every test project I touched (paste the `Passed!` lines below)
- [ ] `dotnet build src/AwesomeRpaUtils.sln` succeeds with 0 warnings
- [ ] Anything that can only be checked on a Windows desktop was verified manually (describe how)

## Checklist
- [ ] Follows the conventions in [CONTRIBUTING.md](../CONTRIBUTING.md) (never throws, unique signatures, `[Category]`/`[Description]`, standalone components, XML docs)
- [ ] Updated the affected component's `README.md` / `Documentation/` (and `CrossReference.md`) if the public API changed
