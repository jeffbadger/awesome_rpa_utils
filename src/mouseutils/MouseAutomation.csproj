<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0-windows</TargetFramework>
    <LangVersion>latest</LangVersion>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>disable</Nullable>
    <AssemblyName>MouseAutomation</AssemblyName>
    <RootNamespace>MouseAutomation</RootNamespace>
    <Platforms>AnyCPU;x64</Platforms>
    <!-- Emit MouseAutomation.xml next to the DLL so the XML doc comments are consumable -->
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
  </PropertyGroup>

  <!-- The test project lives in a subfolder; keep its sources out of the component DLL. -->
  <ItemGroup>
    <Compile Remove="MouseUtils.Tests/**/*.cs" />
  </ItemGroup>

</Project>